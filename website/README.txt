Since local json can't use ajax, Live Server in VS Code is used here to makes it work.
The json file is from the scrapy program. So it needs to be updated manually.  All the urls
are written in relative path.